"""
Streaming RGB-D training package.

Sub-modules
-----------
streaming.sources     — ordered RGB-D frame sources (dataset readers)
streaming.state       — StreamingTrainState and related dataclasses
streaming.save_worker — AsyncSaveWorker for background PLY/checkpoint saves
"""
